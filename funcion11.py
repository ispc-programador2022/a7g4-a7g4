#FUNCION 11- función p1, retorna la resta de los 2 primero por el 3er parámetros, usando las funciones anteriores.

import resta

def p1(a,b,c):
   resta1 = resta.resta(a,b)
   multip = resta1 * c
   return (multip)

